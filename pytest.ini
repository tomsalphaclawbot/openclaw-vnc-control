[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Integration tests require VNC_HOST to be set; they skip gracefully otherwise.
# To run full suite against a live VNC: VNC_HOST=127.0.0.1 VNC_PORT=5900 VNC_PASSWORD=... pytest
